using ChatApplicationSignalR.Managers;
using Microsoft.AspNetCore.Mvc;

namespace ChatApplicationSignalR.Controllers
{
    //public class UserController : ControllerBase
    //{
    //    private readonly UserManager _userManager;

    //    public UserController(UserManager userManager)
    //    {
    //        _userManager = userManager;
    //    }

    //    [HttpPost]
    //    public async Task<IActionResult> AddUser(string username, string password)
    //    {
    //        var result = await _userManager.AddUserAsync(username, password);
    //        if (result.Succeeded)
    //        {
    //            return Ok($"The User {username} has been created.");
    //        }
    //        else
    //        {
    //            return BadRequest(result.Errors);
    //        }
    //    }
    //}
}
